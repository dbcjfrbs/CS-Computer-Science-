1. 공통점
  - 라이브러리, 프레임워크 모두 다른 사람이 쓴 코드를 가져다 쓰는 것이다.

2. 차이점
 - 누구 누구를 컨트롤 하는가 : 내가 코드를 컨트롤 한다면 라이브러리, 정해진 규칙에 따라 짠다면 프레임워크
 - 라이브러리 예시 : JQUERY - 내가 필요할 때 Jquery를 호출하고 내가 코딩해나간다. 다른 라이브러리로 쉽게 대체할 수 있다.
 - 프레임워크 예시 : 프레임워크가 어디에 코드를 어떻게 넣는지(템플릿, 컨트롤러, 뷰...) 알려준다. 이렇게 규칙에 따라 하면된다.
                    django - 내가 장고 문서를 보면서 장고 규칙에 따라 코드를 잘 넣어두어야 제대로 작동한다.
                    
3. 주의점 - 리액트, vue의 경우
 - app에서 ui를 빌드할 때 리액트를 가져오는 것이니 이런 측면에서는 라이브러리다.
   그리고 규칙, 폴더 구조, 컴포넌트명 이런게 없다.
 - 하지만 리액트가 우리의 컴포넌트를 부르고 규칙(props 등)을 알려준다. 이 경우 리액트를 프레임워크로 부를 수 있다. 
